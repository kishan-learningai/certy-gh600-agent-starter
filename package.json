{
  "name": "certy-gh600-agent-starter",
  "version": "1.0.0",
  "description": "A runnable, deterministic agent starter for GH-600 (no API keys needed).",
  "type": "module",
  "scripts": {
    "start": "node --import tsx src/index.ts",
    "test": "node --import tsx --test \"tests/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "author": "Certy (Coded Vision Design)",
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.23.1"
  }
}
